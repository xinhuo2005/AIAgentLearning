[project]
name = "ai-dev-team"
version = "0.1.0"
description = "Multi-Agent Dev Team with Persistent Learning Memory"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.40.0",
    "openai>=1.60.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.28.0",
    "pydantic>=2.10.0",
]
